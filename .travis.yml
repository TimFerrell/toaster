language: node_js
node_js:
  - "0.10"

install:
  - npm install -g gulp mocha browserstack-runner
  - npm install

script:
  - npm test
  - npm browserstack-test

cache:
  directories:
  - node_modules

branches:
  only:
    - develop
    - master